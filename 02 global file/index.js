// const fs = require("fs");
// fs.writeFileSync("hello.txt", "hello how are u ........");

const fs = require("fs").writeFileSync(
  "hy.txt",
  "hello how are u ..... new ff..."
);
//fs buffer,http -core module
// global->console
//non global module -> import fs=file-system
